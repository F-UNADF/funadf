.breadcrumbs
  .page-header
    %h1 Mes votes

.content.mt-3
  .container-fluid
    .card
      .card-header
        %strong Liste des bulletins de votes

      .card-body
        .alert.alert-info
          %p Vous allez voir l'ensemble des bulletins de votes pour lesquels vous ou les églises dont vous êtes présidents.


        .table-responsive
          %table.table.table-striped.table-sm
            %thead
              %tr
                %th Structure concernée
                %th Bulletin
                %th

            - @campaigns.each do |campaign|
              - if campaign.user_can_vote(current_user) || campaign.users_churches_can_vote(current_user)
                %tr
                  %td= campaign.structure_name
                  %td= campaign.name
                  %td
                    = link_to campaign_path(campaign.id), class: 'btn btn-sm btn-primary' do
                      Commencer le vote
                      = icon 'arrow-right'

        = will_paginate @member_campaigns, params: :member_page


