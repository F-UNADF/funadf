.breadcrumbs
  .page-header
    %h1 Gestion des structures


.content.mt-3
  .container-fluid
    .card
      .card-header
        %strong Ajouter une structure

      .card-body
        = render 'form', structure: @structure, action: @structure.becomes(Structure)